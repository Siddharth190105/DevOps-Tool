apiVersion: apps/v1
kind: Deployment
metadata:
  name: green-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myapp
      version: green
  template:
    metadata:
      labels:
        app: myapp
        version: green
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
        ports:
        - containerPort: 80
        command: ["/bin/sh", "-c"]
        args:
          - echo "<html><body style='background-color:green; color:white; text-align:center;'><h1>GREEN DEPLOYMENT</h1></body></html>" > /usr/share/nginx/html/index.html && nginx -g 'daemon off;'
